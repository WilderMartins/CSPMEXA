// import React from 'react';
// import { render, screen } from '@testing-library/react';
// import ProactiveRecommendationsDisplay from './ProactiveRecommendationsDisplay';

// const mockRecommendations = [
//   { id: '1', recommendation: 'Enable MFA for all users', priority: 'High' },
//   { id: '2', recommendation: 'Encrypt all S3 buckets', priority: 'Medium' },
// ];

// describe('ProactiveRecommendationsDisplay', () => {
//   it('renders the display with the provided recommendations', () => {
//     render(<ProactiveRecommendationsDisplay recommendations={mockRecommendations} />);
//     expect(screen.getByText(/Enable MFA for all users/i)).toBeInTheDocument();
//     expect(screen.getByText(/High/i)).toBeInTheDocument();
//     expect(screen.getByText(/Encrypt all S3 buckets/i)).toBeInTheDocument();
//     expect(screen.getByText(/Medium/i)).toBeInTheDocument();
//   });
// });
it.todo('should be tested');
