# Database
DATABASE_URL="postgresql://user:password@localhost:5432/authdb_mvp"

# JWT
JWT_SECRET_KEY="YOUR_VERY_STRONG_JWT_SECRET_KEY_HERE" # Deve ser uma string longa e aleatória
# ACCESS_TOKEN_EXPIRE_MINUTES=1440 # 1 dia (opcional, já tem default em config.py)

# Google OAuth
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID_FROM_GOOGLE_CLOUD_CONSOLE"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET_FROM_GOOGLE_CLOUD_CONSOLE"
# GOOGLE_REDIRECT_URI="http://localhost:8000/api/v1/auth/google/callback" # Opcional; callback para ESTE auth-service.
FRONTEND_URL_AUTH_CALLBACK="http://localhost:3000/auth/callback"      # Onde o frontend recebe o token JWT após login SEM MFA.
FRONTEND_URL_MFA_SETUP="http://localhost:3000/mfa-setup"              # Página no frontend para o usuário configurar o MFA.
FRONTEND_URL_MFA_REQUIRED="http://localhost:3000/mfa-login"           # Página no frontend para inserir o código TOTP durante o login.

# TOTP
# TOTP_ISSUER_NAME="MyAwesomeApp" # Opcional, já tem default no config.py
